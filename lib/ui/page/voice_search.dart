import 'package:flutter/material.dart';

class VoiceSearch extends StatelessWidget {
  const VoiceSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("ترجمه صوتی"),
      ),
    );
  }
}
