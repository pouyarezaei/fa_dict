import 'package:flutter/material.dart';

class WordSearch extends StatelessWidget {
  const WordSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("ترجمه لغت"),),
    );
  }
}
